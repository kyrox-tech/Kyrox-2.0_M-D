name: kyrox 2.0👺
services:
  - name: Kyrox 2.0 service 
    type: web
    ports:
      - 5000
    routes:
      - path: "/"
    build:
      type: docker
      dockerfile: ./Dockerfile
    regions:
      - fra
      - sin


      
